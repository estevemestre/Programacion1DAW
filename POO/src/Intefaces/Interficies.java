// No se poden instanciar
// Tots els metodes son public i abstract es a dir no se implementen
// No tenen variables
// DOmes tenen metedos abstractes i costants  Una constante es una variable del sistema que mantiene un valor inmutable a lo largo de toda la vida del programa. Las constantes en Java se definen mediante el modificador final.  static final nombreConstante = valor;
// Tots el metodes son abstractes a diferencia de les clases abstract poden tindre metodes abstract o no 

package Intefaces;

public class Interficies {

	
	
	// implements  = (extends ) intefaz1 , Intefarz2
}
